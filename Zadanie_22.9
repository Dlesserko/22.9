
b = input('Введите только цифры через пробел - ')
# Это блок проверки на ввод только цифр первой строки с числами через пробел
# Тут типо любые символы не принимаются кроме цифирь
b=b+' '
d=b
d=d.replace(' ','')
while d.isdigit()==False:
    print('Вы ввели неверные символы!\nПовторите ввод только цифр через пробел!\n')
    d = input('Ввод - ')
    d=d+' '
    b=d
    d=d.replace(' ','')
b=list(map(int, b.split()))

a=input('Введите одно целое число - ')
# Это проверка вводимого числа
# Тут как и первом варике проверяются все символы и прнимается ток 1 цифра
while a.isdigit()==False:
    print('Вы ввели не число!')
    a = input('Введите одно целое число - ')
a=int(a)

# Этот блок сортировка чисел в массиве.
# Без метода sorted или sort
c=[]
for i in range(len(b)):
    c.append(min(b))
    b.remove(min(b))
print('Ниже ваш массив из введных чисел в первой строке\n', c)
count=0

# Ну а здесь собственно поиск номера позиции числа
# Как я понял из задания 22.2 пункт 3, это число должно быть
# на позиции где предыдущее из списка меньше, а следующее больше
# Можно конечно еще это число туда воткнуть между двумя искомыми методом insert()
# но в задании этого не было)))
# Да, и я тут сделал все по своему, Извините )-
for i in c:
    if a > max(c):
        print('Вашего число превышает максимальное значение чисел в массиве')
        break
    if a >= i and a<=c[count+1]:
        print(f'Номер позиции числа пользователя между {count+1} и {count+2} позицией')
    count+=1
